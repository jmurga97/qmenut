import { defineConfig } from "vite";

// Consumed by Storybook's @storybook/web-components-vite framework builder.
// ?inline CSS imports and relative-path resolution work with Vite's defaults;
// experimentalDecorators/useDefineForClassFields are picked up automatically
// from ./tsconfig.json via Vite's esbuild tsconfig discovery.
export default defineConfig({});
